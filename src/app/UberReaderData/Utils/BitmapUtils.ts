export class BitmapUtils
{
    // public static scaleBitmap(originalBitmap:BitmapData, maxWidth:number, maxHeight:number):BitmapData
    // {
    // 	if (originalBitmap.width > maxWidth || originalBitmap.height > maxHeight)
    // 	{
    // 		var scaleRatio:number = Math.min(maxWidth / originalBitmap.width, maxHeight / originalBitmap.height);
    // 		var scaledBitmapData:BitmapData = new BitmapData(originalBitmap.width * scaleRatio, originalBitmap.height * scaleRatio);
    // 		var scaleMatrix:Matrix = new Matrix();
    // 		scaleMatrix.scale(scaleRatio, scaleRatio);
    // 		scaledBitmapData.draw(originalBitmap, scaleMatrix, null, null, null, true);
    // 		return scaledBitmapData;
    // 	}
    // 	else
    // 	{
    // 		return originalBitmap;
    // 	}
    // }
}
